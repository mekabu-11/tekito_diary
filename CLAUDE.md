# プロジェクトルール — tekito_diary

## テスト必須ポリシー

このプロジェクトでは、コード変更時にテストの作成・更新を必須とする。

### 機能追加時
- 新しいAPIルートを追加した場合、対応するテストファイルを `web/src/__tests__/` に作成すること
- 新しい lib ユーティリティを追加した場合、ユニットテストを作成すること
- テスト観点: 認証チェック（401/403）、正常系、異常系（空入力・null・エラー）、エッジケース

### バグ修正時
- 修正対象のバグを再現するテストケースを先に追加し、修正後にパスすることを確認すること
- 既存テストの期待値が変わる場合は、テストも合わせて更新すること

### リファクタリング時
- 既存のテストがすべてパスすることを確認してからコミットすること
- インターフェースが変わった場合はテストも更新すること

### コミット前チェック
- `cd web && npm test` を実行し、全テストがパスすることを確認すること
- `npm run build` (`vitest run && next build`) が成功することを確認すること

## テストの書き方

- テストファイルは `web/src/__tests__/` ディレクトリに配置する
- ファイル命名規約: `{テスト対象}.test.ts`
  - APIルート: `api-{ルート名}.test.ts` (例: `api-brushup.test.ts`)
  - ライブラリ: `{モジュール名}.test.ts` (例: `diary-versions.test.ts`)
- テストフレームワーク: Vitest
- 外部依存（Supabase, OpenAI）はモックすること
- テストは日本語で記述すること（`it("未認証の場合 401 を返す", ...)`）

## 技術スタック
- Next.js 16 (App Router)
- TypeScript
- Tailwind CSS 4
- Supabase (Auth / Database / RLS)
- OpenAI API (gpt-5.4-mini)
- Vitest (テスト)
